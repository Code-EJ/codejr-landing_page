# Public form access key generated for email.
# Copy to .env.local, fill in the key and restart Vite / rebuild.
# Never put mailbox passwords or SMTP secrets in a VITE_ variable.
VITE_WEB3FORMS_ACCESS_KEY=
